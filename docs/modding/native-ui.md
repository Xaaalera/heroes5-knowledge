---
content_type: explanation
status: draft
faction: inferno
title: 'Нативные окна: почему Visible=true недостаточно'
lang: ru
section: modding
kicker: HEROES V · UNIVERSE
translation: en/modding/native-ui/
description: 'Нативные окна: почему Visible=true недостаточно'
updated: '2026-09-24'
---
# Нативные окна: почему Visible=true недостаточно

Чтобы показать армию в подсказке, игре нужны **шаблон окна, выбранный корень и модель с данными**. У `ArmyWnd` уже было `Visible=true`; пустую модель этот флаг не заполняет.

## Из чего состоит панель армии

В `data.pak/UI/Tooltips/CommonAdvObjTooltip/` находятся:

| Элемент | Назначение и размер |
|---|---|
| ArmyWnd | Контейнер 197×110 |
| ArmyText | Текст армии |
| CreatureFace.1–7 | Семь ячеек портретов |
| Рамка ячейки / CreatureIconPlace | 40×40 / 34×34 |
| CreaturesNumber | Подпись количества |

`MonsterTooltip` и `CommonAdvObjTooltip` уже ссылаются на этот контейнер. Функция отрисовки ищет дочерние элементы **по именам**.

В изученной модели виртуальные слоты `+0x38/+0x3c/+0x40` отвечают за условие наполнения, число элементов и доступ к элементу. Запись занимает 24 байта и содержит ссылку текстуры, числовую и строковую подписи. Эти смещения описывают одну реализацию, а не универсальный C++ ABI.

## Как окно выбиралось в прототипе

Переключения общей функции отрисовки оказалось недостаточно: панели находились, но данные и место под них подходили не всем хранилищам.

Рабочий путь:

1. Зарегистрировать отдельный корень в `typedWindows` копии Universe `UIGameRoot`.
2. Выбрать его по публичному названию объекта **до** штатного наполнения и компоновки.
3. Остальные подсказки оставить на обычном пути.

В таблице прототипа 19 названий для 12 семейств. Переименование или совпадение имён ломает такой выбор; универсальный диспетчер по Type не подтверждён. Отдельное окно тайника бесов наблюдалось в игре, остальные семейства не прошли такой же полный контроль.

## Список армии и карточка существа

| Окно | Что показывает |
|---|---|
| ArmyWnd в подсказке объекта | Несколько портретов с подписями |
| SCreatureInfoTooltip в бою | Характеристики одного типа существа |

Для проекции создаётся справочный descriptor одного существа без героя и реального боевого стека. `CScreenTooltipController` управляет карточкой и ПКМ; при смене грейда обновляются **обе удерживаемые ссылки**. Замены одной картинки недостаточно.

Подробное окно использует `CSimpleCreaturesRotator`. Показанная в нём единица — заданная справочная численность, а не число врагов.

## Почему нужно проверять второй бой

Ссылки окна и проекций принадлежат конкретному владельцу и поколению; после Start или уничтожения экрана они освобождаются. В ранней версии очистка маски обнуляла указатели, но оставляла размеры. Из-за этого второй бой падал. Исправление сбрасывает **все 24 байта** структуры маски.

Перед нативным вызовом проверяются указатель окна, соглашение вызова, владение ссылкой, активный экран и поток. Одного найденного имени функции или таблицы виртуальных методов недостаточно.

## Адреса исследованной сборки

| Назначение | Адрес |
|---|---|
| Отрисовка списка армии | `0x5f8050` |
| Выбор корня до компоновки | `0x5f8800` |
| Источник tooltip боевого экрана | `0x546f40` |
| Создание creature descriptor | `0x4bd550` |
| Создание rotator | `0x789010` |

**Применимость:** только [закреплённая сборка](../reference/universe-build.md); готового SDK нет. Основание — ресурсы, счётчик вызовов и игровые проверки 21–23 сентября 2026. Исходные DLL игры универсальным загрузчиком не заменялись.
